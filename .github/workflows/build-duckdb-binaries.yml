name: Build duckdb.node for all platforms and Node.js versions

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/build-duckdb-binaries.yml'
      - 'package.json'

jobs:
  build-matrix:
    name: Build for ${{ matrix.os }}-${{ matrix.arch }} (Node.js ${{ matrix.node-version }})
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    strategy:
      fail-fast: false  # 1つのジョブが失敗しても他を継続
      max-parallel: 6   # 並行実行数を制限（macOS課金対策）
      matrix:
        include:
          # Linux x64 - 複数のNode.jsバージョン
          - os: ubuntu-22.04
            arch: x64
            platform: linux
            node-version: 18
            abi: 108
          - os: ubuntu-22.04
            arch: x64
            platform: linux
            node-version: 20
            abi: 115
          - os: ubuntu-22.04
            arch: x64
            platform: linux
            node-version: 22
            abi: 127
          - os: ubuntu-22.04
            arch: x64
            platform: linux
            node-version: 23
            abi: 140

          # macOS arm64 (Apple Silicon)
          - os: macos-14
            arch: arm64
            platform: darwin
            node-version: 18
            abi: 108
          - os: macos-14
            arch: arm64
            platform: darwin
            node-version: 20
            abi: 115
          - os: macos-14
            arch: arm64
            platform: darwin
            node-version: 22
            abi: 127
          - os: macos-14
            arch: arm64
            platform: darwin
            node-version: 23
            abi: 140

          # Windows x64
          - os: windows-2022
            arch: x64
            platform: win32
            node-version: 18
            abi: 108
          - os: windows-2022
            arch: x64
            platform: win32
            node-version: 20
            abi: 115
          - os: windows-2022
            arch: x64
            platform: win32
            node-version: 22
            abi: 127
          - os: windows-2022
            arch: x64
            platform: win32
            node-version: 23
            abi: 140

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install Python (for node-gyp)
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Build DuckDB from source (Unix)
        if: runner.os != 'Windows'
        run: |
          npm install
          cd node_modules/duckdb
          npm run install --build-from-source

      - name: Build DuckDB from source (Windows)
        if: runner.os == 'Windows'
        run: |
          npm install
          cd node_modules\duckdb
          npm run install --build-from-source

      - name: Sign binary (macOS only)
        if: runner.os == 'macOS'
        run: |
          echo "Signing DuckDB binary with ad-hoc signature..."
          codesign -s - -f node_modules/duckdb/lib/binding/duckdb.node
          codesign -v node_modules/duckdb/lib/binding/duckdb.node

      - name: Copy and verify duckdb.node (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p bindings
          cp node_modules/duckdb/lib/binding/duckdb.node bindings/duckdb-${{ matrix.platform }}-${{ matrix.arch }}-abi${{ matrix.abi }}.node
          ls -lh bindings/duckdb-${{ matrix.platform }}-${{ matrix.arch }}-abi${{ matrix.abi }}.node
          echo "Built for Node.js ${{ matrix.node-version }} (ABI ${{ matrix.abi }})"
          echo "Output: bindings/duckdb-${{ matrix.platform }}-${{ matrix.arch }}-abi${{ matrix.abi }}.node"

      - name: Copy and verify duckdb.node (Windows)
        if: runner.os == 'Windows'
        run: |
          New-Item -ItemType Directory -Force -Path bindings
          Copy-Item node_modules\duckdb\lib\binding\duckdb.node bindings\duckdb-${{ matrix.platform }}-${{ matrix.arch }}-abi${{ matrix.abi }}.node
          Get-Item bindings\duckdb-${{ matrix.platform }}-${{ matrix.arch }}-abi${{ matrix.abi }}.node
          Write-Output "Built for Node.js ${{ matrix.node-version }} (ABI ${{ matrix.abi }})"
          Write-Output "Output: bindings/duckdb-${{ matrix.platform }}-${{ matrix.arch }}-abi${{ matrix.abi }}.node"

      - uses: actions/upload-artifact@v4
        with:
          name: duckdb-${{ matrix.platform }}-${{ matrix.arch }}-abi${{ matrix.abi }}
          path: bindings/duckdb-${{ matrix.platform }}-${{ matrix.arch }}-abi${{ matrix.abi }}.node
          retention-days: 90
